<UserControl x:Class="SSNSWorks.uc메인화면"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:ctrls="clr-namespace:SSControlsNET;assembly=SSControlsNET"
             xmlns:local="clr-namespace:SSNSWorks"
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="950" 
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="DB설정 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
            <TextBlock Name="txtDBURL" Width="Auto" Height="Auto" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
            <Button x:Name="btnDB찾기" Content="선택" Width="60" Height="20" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btnDB찾기_Click"/>
            <Button x:Name="btnDB연결" Content="접속" Width="60" Height="20" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btnDB연결_Click"/>
            <Button x:Name="btn정보가져오기" Content="가져오기" Width="60" Height="20" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btn정보가져오기_Click"/>            
        </StackPanel>
        <TabControl x:Name="tabMain" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TabItem Header="기본">
                <local:uc기본설정 x:Name="_u기본" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" On데이터변경Event="_u기본_On데이터변경Event"/>
            </TabItem>
            <TabItem Header="화면">
                <local:uc화면설정 x:Name="_u화면" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" On데이터변경Event="_u화면_On데이터변경Event" On데이터요청Event="_u화면_On데이터요청Event"/>
            </TabItem>
            <TabItem Header="프로그램">
                <local:uc프로그램설정 x:Name="_u프로그램" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" On데이터변경Event="_u프로그램_On데이터변경Event"/>
            </TabItem>
            <TabItem Header="테스트">
                <StackPanel Margin="5">
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="Packet : " Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                        <TextBox x:Name="txt패킷" Width="700" Height="200" TextAlignment="Left" VerticalScrollBarVisibility="Visible" VerticalAlignment="Top" Foreground="{DynamicResource COLOR_FONT_B1}" TextWrapping="WrapWithOverflow"/>
                        <Button x:Name="btn패킷분석" Content="패킷분석" Width="60" Height="50" Margin="5,0,0,0" HorizontalAlignment="Center" Click="btn패킷분석_Click"/>
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
