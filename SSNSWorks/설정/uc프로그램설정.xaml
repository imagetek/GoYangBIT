<UserControl x:Class="SSNSWorks.uc프로그램설정"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:ctrls="clr-namespace:SSControlsNET;assembly=SSControlsNET"
             xmlns:local="clr-namespace:SSNSWorks"
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="950" 
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>        
        <StackPanel Grid.Row="0">
            <GroupBox Header="프로그램 설정" Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="기록 보관일 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nLOGSAVE_DAY" Width="50" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center" />
                            <TextBlock Text="(일)" Width="Auto" Height="Auto" TextAlignment="Left" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="기록 보관비율 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nLOGSAVE_PERCENT" Width="50" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center"/>
                            <TextBlock Text="(%)" Width="Auto" Height="Auto" TextAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                        </StackPanel>                   
                    </StackPanel>
                </Grid>
 
            </GroupBox>
            <GroupBox Header="LED설정" Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="화면전환속도 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nLED_PAGE_CHGTIME" Width="50" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center"/>
                            <TextBlock Text="(초)" Width="Auto" Height="Auto" TextAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="버스번호 색상 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <CheckBox x:Name="USE_BUSNO_COLOR" Content="체크시 사용" Width="Auto" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}" />                            
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="곧도착 버스번호 색상 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <CheckBox x:Name="USE_ARRIVESOON_COLOR" Content="체크시 사용" Width="Auto" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>                            
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="저상버스 표시 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <CheckBox x:Name="USE_BUSNO_LOW" Content="체크시 표시" Width="Auto" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="곧도착 저상버스 표시 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <CheckBox x:Name="USE_ARRIVESOON_LOW" Content="체크시 표시" Width="Auto" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </GroupBox>
            <GroupBox Header="환경보드 관련" Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="FAN수동제어 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <CheckBox x:Name="USE_FAN_MANUAL" Content="체크시 수동제어" Width="Auto" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="HEATER수동제어 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <CheckBox x:Name="USE_HEATER_MANUAL" Content="체크시 수동제어" Width="Auto" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </GroupBox>

        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
            <Button x:Name="btn설정저장" Content="설정저장" Width="60" Height="25" Margin="0,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Click="btn설정저장_Click"/>
            <!--<Button x:Name="btn설정삭제" Content="설정삭제" Width="60" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Click="btn설정삭제_Click"/>-->
        </StackPanel>
        <!--<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="(설정값 우선순위 ▷ 설정파일 , DB 순 입니다 , 설정삭제 시 설정파일이 제거됩니다.)" Width="Auto" Height="Auto" Margin="0,0,5,0" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
        </StackPanel>-->

    </Grid>
</UserControl>
