<UserControl x:Class="BitView.uc화면설정"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctrls="clr-namespace:SSControlsNET;assembly=SSControlsNET"
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:BitView"
             mc:Ignorable="d" d:DesignHeight="650" d:DesignWidth="950" 
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="125" />
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <GroupBox Header="화면설정" Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="화면구분 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <ComboBox x:Name="cmbDISP_GBN" Width="250" Height="20" Margin="5,0,0,0" MaxDropDownHeight="200" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" SelectionChanged="cmbDISP_GBN_SelectionChanged" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="시작위치 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nPOS_X" Width="100" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center"/>
                            <TextBlock Text=" ," Width="Auto" Height="Auto" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nPOS_Y" Width="100" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center"/>
                        </StackPanel>
                    </StackPanel >
                    <!--<StackPanel Grid.Column="1">
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="화면이름 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <TextBox Name="txtDISP_NM" Width="200" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="화면크기 :" Width="150" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nSZ_W" Width="100" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center"/>
                            <TextBlock Text=" x" Width="Auto" Height="Auto" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <toolkit:DoubleUpDown x:Name="nSZ_H" Width="100" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" CultureInfo="ko-KR" InputScope="Number" ParsingNumberStyle="Integer" TextAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>-->
                </Grid>
            </GroupBox>
            <StackPanel Margin="0,5,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btn화면추가" Content="추가" Width="60" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btn화면추가_Click"/>
                <Button x:Name="btn화면수정" Content="수정" Width="60" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btn화면수정_Click"/>
                <Button x:Name="btn화면삭제" Content="삭제" Width="60" Height="25" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="btn화면삭제_Click"/>
            </StackPanel>
        </StackPanel>
        <DataGrid x:Name="dg화면" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single"  CanUserAddRows="False" SelectionUnit="FullRow" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled"
                  Grid.Row="1" Margin="5" BorderThickness="0" ColumnHeaderStyle="{DynamicResource ssStyleDataGridColumnHeader}" CellStyle="{DynamicResource ssStyleDataGridCell}" RowStyle="{DynamicResource ssStyleDataGridRow}" 
                  VerticalGridLinesBrush="{DynamicResource COLOR_구분선B}" HorizontalGridLinesBrush="{DynamicResource COLOR_구분선C}" MouseDoubleClick="dg화면_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="종류" Binding="{Binding 표시화면}" Width="Auto" />
                <DataGridTextColumn Header="화면명" Binding="{Binding DISP_NM}" Width="Auto" />
                <DataGridTextColumn Header="시작위치X" Binding="{Binding POS_X}" Width="Auto" />
                <DataGridTextColumn Header="시작위치Y" Binding="{Binding POS_Y}" Width="Auto" />
                <!--<DataGridTextColumn Header="가로크기" Binding="{Binding SZ_W}" Width="Auto" />
                <DataGridTextColumn Header="세로크기" Binding="{Binding SZ_H}" Width="Auto" />-->
            </DataGrid.Columns>
        </DataGrid>
       
    </Grid>
</UserControl>
