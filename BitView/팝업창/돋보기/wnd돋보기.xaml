<Window x:Class="BitView.wnd돋보기"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrls="clr-namespace:SSControlsNET;assembly=SSControlsNET"
        xmlns:local="clr-namespace:BitView"
        mc:Ignorable="d" Title="돋보기" Width="640" Height="480"
        Style="{DynamicResource wDialogStyleNoButtons}" ShowInTaskbar="True"
        Loaded="Window_Loaded" Closing="Window_Closing" SizeChanged="Window_SizeChanged" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal" HorizontalAlignment="Center" >
            <TextBlock Text="돋보기" FontSize="18" TextAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource COLOR_FONT_A}" VerticalAlignment="Center" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" >
            <TextBlock Text="배율 :" Width="35" TextAlignment="Right" Foreground="{DynamicResource COLOR_FONT_A}" VerticalAlignment="Center" />
            <TextBlock x:Name="txt배율" Text="2" Width="Auto" TextAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" VerticalAlignment="Center" />
            <Button x:Name="btn확대" Content="△" Width="25" Height="25" Margin="5,0,0,0" Click="btn확대_Click"/>
            <Button x:Name="btn축소" Content="▽" Width="25" Height="25" Margin="5,0,0,0" Click="btn축소_Click"/>            
        </StackPanel>
        <!--<Border x:Name="bdRow2" Grid.Row="2" BorderBrush="DodgerBlue" BorderThickness="1" Margin="1">-->
            <Image x:Name="img" Grid.Row="2" Margin="0" Width="640" Height="400" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>        
        <!--</Border>-->
        <Grid Grid.Row="3" x:Name="gridGrip" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="20" Height="20" Cursor="SizeNWSE" Background="#02000000" MouseLeftButtonDown="gridGrip_MouseLeftButtonDown" MouseMove="gridGrip_MouseMove" MouseLeftButtonUp="gridGrip_MouseLeftButtonUp">
            <Image Source="/SSResource;component/images/그립아이콘B.png" Stretch="Fill" />
        </Grid>
    </Grid>
</Window>
