<Window x:Class="BitView.wndBIT설정"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrls="clr-namespace:SSControlsNET;assembly=SSControlsNET"
        xmlns:local="clr-namespace:BitView"
        mc:Ignorable="d" Title="BIT기본설정" Width="570" Height="450" 
        Style="{DynamicResource wDialogStyleNoButtons}"
        Loaded="Window_Loaded" Closing="Window_Closing" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="1" />
            <RowDefinition />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="5" Orientation="Horizontal" HorizontalAlignment="Center" >
            <TextBlock Text="BIT 기본설정" FontSize="18" TextAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource COLOR_FONT_A}" VerticalAlignment="Center" />
        </StackPanel>

        <Rectangle Fill="{DynamicResource COLOR_구분선B}" Grid.Row="1"  />

        <TabControl Grid.Row="2" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TabItem Header="기본">
                <StackPanel>
                    <GroupBox Header="설치장소" Margin="5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="BIT ID :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtBIT_ID" Width="150" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" ctrls:TextBoxMaskBehavior.Mask="Integer"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="모바일No :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtMOBILE_NO" Width="150" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="정류장명 :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtSTATION_NM" Width="300" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                            </StackPanel>                           
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="환경보드" Margin="5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="시리얼포트 :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <ComboBox x:Name="cmbENV2_PORT" Width="100" Height="20" Margin="5,0,0,0" MaxDropDownHeight="200" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
                                <TextBlock Text="통신속도 :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtENV_BAUD_RATE" Width="100" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" ctrls:TextBoxMaskBehavior.Mask="Integer"/>
                            </StackPanel>                            
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="웹캠" Margin="5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="웹캠설정 :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <ComboBox x:Name="cmbWEBCAM" Width="200" Height="20" Margin="5,0,0,0" MaxDropDownHeight="200" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />                                
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="충격용설정 :" Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <ComboBox x:Name="cmbSHOCKCAM" Width="200" Height="20" Margin="5,0,0,0" MaxDropDownHeight="200" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                  
                </StackPanel>
            </TabItem>
            <TabItem Header="서버">
                <StackPanel>
                    <GroupBox Header="서버정보" Margin="5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="서버 IP : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtSERVER_IPLIST" Text="" Width="350" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="서버 Port : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtSERVER_PORTLIST" Text="" Width="350" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="(여러개의 서버IP , Port 입력시 | 로 구분해서 입력해주세요.)" Width="Auto" Height="Auto" Margin="110,0,0,0" TextAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_닫기_PRESSED}"/>                                
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="SFTP정보" Margin="5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="FTP 종류 : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <ComboBox x:Name="cmbFTP_GBN" Width="150" Height="20" Margin="5,0,0,0" MaxDropDownHeight="200" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="FTP IP : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtFTP_IP" Text="" Width="250" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                                <TextBlock Text="FTP Port : " Width="70" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtFTP_PORT" Text="" Width="55" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" ctrls:TextBoxMaskBehavior.Mask="Integer"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="FTP ID : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <TextBox Name="txtFTP_ID" Text="" Width="250" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="FTP PWD : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                                <PasswordBox Name="txtFTP_PWD" Width="250" Height="Auto" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" HorizontalContentAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="HTTP정보" Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="HTTP IP : " Width="100" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <TextBox Name="txtHTTP_IP" Text="" Width="250" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}"/>
                            <TextBlock Text="HTTP Port : " Width="70" Height="Auto" TextAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_B1}"/>
                            <TextBox Name="txtHTTP_PORT" Text="" Width="55" Height="Auto" Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource COLOR_FONT_A}" ctrls:TextBoxMaskBehavior.Mask="Integer"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>
        </TabControl>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btn저장" Content="저장" Width="60" Height="25" Margin="0,0,5,0" VerticalAlignment="Center" HorizontalAlignment="Right" Click="btn저장_Click"/>
            <Button x:Name="btn닫기" Content="닫기" Width="60" Height="25" Margin="0,0,5,0" VerticalAlignment="Center" HorizontalAlignment="Right" Click="btn닫기_Click"/>
        </StackPanel>
    </Grid>
</Window>